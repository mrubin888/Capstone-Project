<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <EditText android:id="@+id/edit_project_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/project_title_hint"
        android:maxLines="1"
        android:inputType="text"/>

    <EditText android:id="@+id/edit_project_description"
        android:layout_below="@id/edit_project_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/project_description_hint"
        android:maxLines="12"
        android:inputType="textMultiLine"/>

    <Button
        android:id="@+id/create_project_button"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/create_project"
        android:textColor="#FFFFFF"
        android:background="@color/colorPrimary"/>

</RelativeLayout>